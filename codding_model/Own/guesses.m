function guess=guesses(varsModel, indic, opt_pol)
% function to read in initial guesses for ramsey problem solution

if t==1 % initial period
 


else % all other periods
    tauul = opt_pol(varsModel=='tauul');
end